version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: therahulpatil-portfolio
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
